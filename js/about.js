import { initMenu } from './hero.js'; initMenu(); if (!matchMedia('(prefers-reduced-motion: reduce)').matches) { const { gsap } = await import('../assets/vendor/gsap/index.js'); const { ScrollTrigger } = await import('../assets/vendor/gsap/ScrollTrigger.js'); gsap.registerPlugin(ScrollTrigger); gsap.from('[data-about-reveal]', { opacity: 0, y: 18, duration: .8, ease: 'power3.out', stagger: .1 }); document.querySelectorAll('[data-about-section]').forEach((element) => gsap.from(element, { opacity: .85, y: 12, duration: .9, ease: 'power3.out', scrollTrigger: { trigger: element, start: 'top 78%', once: true } })); }
